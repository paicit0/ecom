import React from "react";
import { Pressable, StyleSheet, TextInput, View, Text } from "react-native";

const Slider = (list: string[]) => {
    return (
        <View>
            // list maps to pressables to make slider for pressables
        </View>
    )
}


export default Slider;